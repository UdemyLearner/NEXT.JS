./tsconfig.json   -> is used to define what should be type checked, ignored and rules to follow

./

./postcss.config.mjs -> config file for post CSS. It is used to process CSS with different plugins. Allows to use utility first classes as plugins

./next-env.d.ts -> typescript declaration for next.js 

./next.config.ts -> config next.js features


